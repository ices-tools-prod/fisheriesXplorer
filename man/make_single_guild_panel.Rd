% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_stock_status.R
\name{make_single_guild_panel}
\alias{make_single_guild_panel}
\title{Create panel for a single guild (stock-level lines + mean)}
\usage{
make_single_guild_panel(
  df2,
  mean_df,
  df_all_years,
  metric_raw,
  yaxis_title,
  legend_stocks,
  values,
  ct_group,
  show_mean_in_legend = TRUE
)
}
\arguments{
\item{df2}{Data frame of non-mean stock observations.
Must include: \code{Year}, \code{Value}, \code{Metric}, \code{StockKeyLabel},
\code{Proxy_is_proxy}, \code{hover}.}

\item{mean_df}{Data frame containing mean time series (StockKeyLabel == "Mean").}

\item{df_all_years}{Data frame used only to determine x-axis range (Year).}

\item{metric_raw}{Character string. Metric identifier.}

\item{yaxis_title}{Character string. Label for the y-axis.}

\item{legend_stocks}{Character vector of stock IDs to include in the legend.
Other stocks are plotted but hidden from the legend.}

\item{values}{Named character vector mapping stock IDs to colors.}

\item{ct_group}{Character string. Crosstalk group ID for linked highlighting.}

\item{show_mean_in_legend}{Logical. Whether to include the mean line in the legend.}
}
\value{
A Plotly object representing a single panel.
}
\description{
Generates a Plotly panel for a selected fisheries guild, showing:
individual stock trajectories and the guild-level mean.
}
\details{
This helper implements the main plotting logic for individual guilds:
\itemize{
  \item Splits stocks into proxy vs non-proxy:
    \itemize{
      \item Solid lines = observed data
      \item Dotted lines = proxy-based data
    }
  \item Separates legend-visible and legend-hidden traces
  \item Uses \code{highlight_key()} to enable cross-panel interaction
  \item Applies dimming for unselected traces
  \item Adds a thick black mean line
  \item Adds reference line at y = 1 and panel border
}

The function assumes that proxy status is constant within each stock time series.
}
