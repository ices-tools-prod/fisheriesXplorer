% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_stock_status.R
\name{make_all_guilds_panel}
\alias{make_all_guilds_panel}
\title{Create panel for "All guilds" view (mean lines only)}
\usage{
make_all_guilds_panel(
  df_mean,
  metric_raw,
  yaxis_title,
  colors,
  showlegend = TRUE
)
}
\arguments{
\item{df_mean}{Data frame containing mean time series for each guild.
Must include columns: \code{Year}, \code{Value}, \code{Metric}, \code{FisheriesGuild}.}

\item{metric_raw}{Character string. Metric identifier (e.g. "F_FMSY", "SSB_MSYBtrigger").}

\item{yaxis_title}{Character string. Label for the y-axis (HTML allowed).}

\item{colors}{Named character vector mapping \code{FisheriesGuild} to colors.}

\item{showlegend}{Logical. Whether to display legend entries for this panel.}
}
\value{
A Plotly object representing a single panel.
}
\description{
Generates a single Plotly panel showing one line per fisheries guild,
using pre-computed mean values (StockKeyLabel == "Mean").
}
\details{
This helper is used when \code{guild == "All"}:
\itemize{
  \item Only mean series are plotted (no individual stocks)
  \item Each guild is represented as one line
  \item Lines are grouped and colored by \code{FisheriesGuild}
  \item A horizontal reference line at y = 1 is added
  \item A bounding box frame is drawn around the panel
}

Hover text includes guild, year, and metric value.
}
